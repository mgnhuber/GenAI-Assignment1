# GenAI-Assignment1 - Megan Huber

This repository contains work for Generative AI class's Assignment 1 at Johns Hopkins University, including short written deliverables related to industry use of AI. It also serves as a simple example of creating files, tracking changes with Git, and pushing commits to GitHub.

Git steps used: create or update files in the repository, check the repository status, stage the intended file with `git add`, commit the change with `git commit -m`, and push the branch to GitHub with `git push origin main`.

Video Link: https://youtu.be/hs-rYruewnE

Recent commit summary:
- `a7f6255` Update README with repository summary
- `66fd1fe` Add final version markdown
- `6bc4328` Add assistant draft markdown
- `e4d34ef` Initial commit
